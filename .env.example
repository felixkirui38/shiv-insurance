# Server
PORT=5000
NODE_ENV=development

# CMS admin login (required)
CMS_USERNAME=
CMS_PASSWORD=

# Session encryption (required — use a long random string)
SESSION_SECRET=

# Set to "true" only when serving over HTTPS in production
COOKIE_SECURE=false

# Email delivery (required for lead + confirmation emails)
# Without these, forms still save to CMS but no emails are sent.
SMTP_HOST=mail.shivinsbro.co.ke
SMTP_PORT=465
SMTP_USER=info@shivinsbro.co.ke
SMTP_PASS=
SMTP_FROM=info@shivinsbro.co.ke
# Optional: set to false if cPanel TLS certificate verification fails
# SMTP_TLS_REJECT_UNAUTHORIZED=false

# Database
# Production / Coolify (internal Docker network hostname — set this in Coolify env vars):
# DATABASE_URL=postgres://USER:PASSWORD@INTERNAL_HOST:5432/postgres
#
# Local development (Coolify public IP/domain + mapped port, often 3000):
# DATABASE_URL=postgres://USER:PASSWORD@YOUR_COOLIFY_SERVER_IP:3000/postgres?sslmode=disable
DATABASE_URL=

# --- Coolify / production deployment ---
# In Coolify → Application → Environment Variables, set at minimum:
#   DATABASE_URL    (use the internal postgres://... URL from Coolify)
#   SESSION_SECRET  (long random string)
#   CMS_USERNAME    (e.g. shiv_2026)
#   CMS_PASSWORD    (your CMS password)
#   COOKIE_SECURE=true   (when using HTTPS on shivinsbro.co.ke)
#   SMTP_HOST / SMTP_PORT / SMTP_USER / SMTP_PASS / SMTP_FROM
# Port: expose container port 5000 (matches PORT above; Coolify proxy routes to this)
